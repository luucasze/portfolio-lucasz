import "./globals.css";

export const metadata = {
  title: "Kit Defesa | Pare de ser desvalorizado como freelancer",
  description:
    "Sem contrato, sem precificação, sem proteção. Descubra como se posicionar, cobrar certo e parar de ser passado pra trás.",
  openGraph: {
    title: "Kit Defesa",
    description: "Você não ganha mal. Você trabalha sem estrutura.",
    images: ["/og-image.jpg"],
  },
};

export default function RootLayout({ children }) {
  return (
    <html lang="pt-BR">
      <body className="bg-black text-white antialiased">
        {children}
      </body>
    </html>
  );
}