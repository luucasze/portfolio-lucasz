import Container from "@/components/Container";
import SectionWrapper from "@/components/SectionWrapper";
import { inclusos } from "@/content/inclusos";

export default function Inclusos() {
  return (
    <SectionWrapper className="bg-black text-white">
      <Container>
        <div className="flex flex-col gap-10">

          {/* Título */}
          <h2 className="text-3xl md:text-5xl font-bold leading-tight max-w-2xl">
            {inclusos.title}
          </h2>

          {/* Lista */}
          <div className="grid md:grid-cols-2 gap-6">

            {inclusos.items.map((item, index) => (
              <div
                key={index}
                className="border border-neutral-800 rounded-2xl p-6 bg-neutral-900/40 flex flex-col gap-2"
              >
                <h3 className="text-lg font-semibold text-white">
                  {item.title}
                </h3>

                <p className="text-gray-300 text-sm leading-relaxed">
                  {item.desc}
                </p>
              </div>
            ))}

          </div>

        </div>
      </Container>
    </SectionWrapper>
  );
}